import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#36B399",
      // main: "#10a37f",
    },
    secondary: {
      main: "#ccc",
    },
  },
});

export default theme;
